Checklist pour commencer avec GIT.

git add pour ajouter un fichier dans la track list
git commit -a -m "Commentaire" 
/* -a pour mettre à jour le track list,
** -m pour ajouter un message explicite
*/
